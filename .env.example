# Local development environment values. Copy to .env (which is gitignored).
# In production these come from the platform (e.g. Cloud Run injects PORT).
#
# Variable expansion is supported, e.g.:
#   HOST=localhost
#   BASE_URL=http://${HOST}:${PORT}

# Port the server listens on (and the dev proxy targets).
PORT=8080
