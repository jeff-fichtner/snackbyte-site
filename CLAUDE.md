# CLAUDE.md — snackbyte-site

## Client sections

`src/web/public/work/<slug>/` holds self-contained client sections, published from
other repos — sometimes by agents that never open this one. Before touching
anything under `work/`, read `docs/SECTIONS.md`; it is the contract, and it binds
contributors from outside this directory as much as work done inside it.

The short version: a contributor writes only inside its own section folder, pages
are standalone HTML, `section.json` is the manifest, and the section's root
`index.html` is generated by `scripts/build-sections.mjs` on every build.

Sections are never linked from the homepage.

<!-- SPECKIT START -->
This project uses spec-driven development (GitHub Spec Kit). Read
`.specify/memory/constitution.md` first — it governs this repository, and it binds
the owner's global rules and `snackbyte-base`'s constitution by reference rather
than restating them. A feature gets a spec (`/speckit-specify`); a chore does not.
Plans live under `specs/`.
<!-- SPECKIT END -->

## Brand

The brand (mark, palette, type, voice, rules) is recorded in `docs/BRAND.md`, with
the readable version live at <https://snackbyte.io/style/>. That file is the record:
a brand decision is made there, then rendered here. `brand/` holds the cut assets,
all generated by `npm run brand:assets` — never hand-edited. `src/web/brand.css` is
the brand's half of the stylesheet (tokens and scales); page styling belongs in
`app.css`. The brand moves to its own npm package at the second consumer.
