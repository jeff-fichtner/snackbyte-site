node_modules/
dist/
coverage/
package-lock.json
*.tsbuildinfo

# Spec-workflow scaffolding — not shipped code, not formatted by the app gate
.specify/
.claude/
specs/
CLAUDE.md

# Standalone published documents — complete HTML pages copied in from the project
# that generates them, not app source. Reformatting them here would fight the generator.
# NB: patterns in this file resolve relative to config/, hence the ../ prefix.
../src/web/public/**
